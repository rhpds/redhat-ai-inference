= Module 1: Deploy a Model on Red Hat AI Enterprise
:navtitle: 1. Deploy a Model

== Overview

In this module you deploy a model for inference on Red Hat AI Enterprise.
You select a model, configure a model server, and verify the deployment is
accepting inference requests. The model and runtime you configure here carry
forward through all subsequent modules.

.Duration
10 minutes

== Learning Objectives

* Deploy a model for inference on Red Hat AI Enterprise
* Verify that the deployed model is running and accepting inference requests

== 1.1 — Explore the Lab Environment

TODO: Describe the lab environment — OpenShift AI console access, project/namespace layout, pre-loaded resources.

== 1.2 — Deploy the Model for Inference

. Log in to the Red Hat AI Enterprise console.
. Navigate to the model serving area within OpenShift AI.
. Review the available model server runtimes.
. Select the model to deploy.
. Configure the model server parameters: runtime, replicas, and resource limits.
. Deploy the model to the platform.
. Monitor the deployment status and wait for the model pod to become `Running`.

== 1.3 — Verify the Deployment

. Locate the model's inference endpoint in the console.
. Send a test inference request to confirm the model is responding.

[source,bash]
----
# TODO: add curl/inference command
----

. Confirm the response matches the expected output format.

== Key Takeaways

* Red Hat AI Enterprise provides a streamlined workflow for deploying models to inference endpoints.
* A deployed model must be verified before it can be evaluated or exposed to consumers.
* Model server configuration — runtime, replicas, and resource limits — determines how the model serves inference requests.

'''

xref:module-02-evaluate-benchmark.adoc[Next: Evaluate and Benchmark Model Performance →]
